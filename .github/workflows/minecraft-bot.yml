name: Minecraft Bot

on:
  workflow_dispatch:
  push:
    paths:
      - 'bot/**'
      - '.github/workflows/minecraft-bot.yml'
  schedule:
    # Refresh the runner before the GitHub-hosted job reaches its time limit.
    # This reduces downtime but cannot guarantee uninterrupted 24/7 hosting.
    - cron: '*/10 * * * *'

concurrency:
  group: minecraft-bot
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: bot

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: bot/package.json

      - name: Install bot dependencies
        run: npm install --no-audit --no-fund

      - name: Start Minecraft bot
        run: npm start
